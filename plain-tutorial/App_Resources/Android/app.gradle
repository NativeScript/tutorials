android {
  compileSdkVersion 36
  buildToolsVersion "36"

  defaultConfig {
    minSdkVersion 24
    targetSdkVersion 36
    generatedDensities = []
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
